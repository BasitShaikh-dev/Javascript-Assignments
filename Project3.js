var car = 'Albert Einstein once said, “A person who never made a mistake never tried anything new.” ';
var low = car.toLowerCase();
var upp = car.toUpperCase();
var tit = car.charAt(0).toUpperCase();

console.log(low);
console.log(upp);
console.log(`${tit} ${car.slice(1)}`);

